# Kleinanzeigen.de Telegram Monitor Bot

**Многопользовательский** Telegram бот для автоматического мониторинга объявлений автомобилей на kleinanzeigen.de.

## 🎯 Возможности

- 👥 **Многопользовательский режим** — каждый может использовать бота с персональными настройками
- 🔍 Мониторинг нескольких URL поиска одновременно
- 📊 Автоматическая проверка каждые N минут (настраивается для каждого пользователя)
- 🚗 Извлечение данных: марка, год, цена, регион, PLZ
- 🔔 Уведомления в Telegram о новых объявлениях
- 🛡️ Защита от повторной отправки (дедупликация по пользователю)
- ⚡ Rate limiting для избежания блокировки

## 🚀 Быстрый старт

### 1. Создание Telegram бота

1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Сохраните токен бота (формат: `123456789:ABC-DEF...`)

> ⚠️ **Важно:** `TG_CHAT_ID` НЕ нужен! Бот автоматически отправляет уведомления каждому пользователю.

### 2. Установка

```bash
cd /Users/moritzmuller/playground/coding/tgkleinzeigen

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 3. Конфигурация

```bash
# Скопируйте шаблон конфигурации
cp .env.example .env

# Укажите только токен бота
nano .env
```

Единственный обязательный параметр:
```env
TG_BOT_TOKEN=ваш_токен_бота
```

### 4. Запуск

```bash
python -m src.main
```

## 📱 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Справка и твой статус |
| `/add <url>` | Добавить URL поиска |
| `/list` | Показать твои активные URL |
| `/remove <id>` | Удалить URL по ID |
| `/interval <мин>` | Изменить твой интервал проверки |
| `/status` | Твоя статистика |
| `/test` | Принудительная проверка |

## 📖 Пример использования

1. Откройте [kleinanzeigen.de](https://www.kleinanzeigen.de)
2. Настройте фильтры поиска (регион, цена, марка и т.д.)
3. Скопируйте URL из адресной строки
4. Отправьте боту: `/add https://www.kleinanzeigen.de/s-autos/...`
5. Бот начнёт мониторинг и пришлёт вам новые объявления

**Каждый пользователь имеет:**
- Свой список URL для мониторинга
- Свой интервал проверки
- Свою историю отправленных объявлений

## 🐳 Развёртывание

### Docker

```bash
# Создайте .env файл с токеном
cp .env.example .env
nano .env

# Запустите
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

### systemd (Linux VPS)

```bash
# Скопируйте файлы
sudo mkdir -p /opt/kleinanzeigen-bot
sudo cp -r . /opt/kleinanzeigen-bot/
cd /opt/kleinanzeigen-bot

# Создайте venv и установите зависимости
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Настройте .env
cp .env.example .env
nano .env

# Создайте директории для данных и логов
sudo mkdir -p /var/log/kleinanzeigen-bot
sudo chown www-data:www-data /var/log/kleinanzeigen-bot

# Установите сервис
sudo cp kleinanzeigen.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable kleinanzeigen
sudo systemctl start kleinanzeigen
```

## 📨 Формат уведомлений

```
🚗 BMW (2016)
📍 Bayern - Amberg
🏷 PLZ: 92224
💶 Цена: 11.350 €

🔗 https://www.kleinanzeigen.de/s-anzeige/...
```

## 📁 Структура проекта

```
tgkleinzeigen/
├── src/
│   ├── config.py      # Конфигурация из ENV
│   ├── database.py    # SQLite (пользователи, запросы, настройки)
│   ├── fetcher.py     # HTTP клиент с retry
│   ├── parser.py      # Парсинг HTML
│   ├── bot.py         # Telegram команды (многопользовательский)
│   ├── scheduler.py   # Планировщик (для всех пользователей)
│   └── main.py        # Точка входа
├── data/              # SQLite база
├── requirements.txt
├── .env.example
├── Dockerfile
├── docker-compose.yml
└── kleinanzeigen.service
```

## База данных

Бот использует SQLite с таблицами:
- `users` — зарегистрированные пользователи
- `queries` — URL поиска (привязаны к пользователю)
- `user_settings` — настройки пользователя (интервал)
- `seen_listings` — отправленные объявления (по пользователю)
- `stats` — статистика проверок

## Лицензия

MIT
